 <%= form_for @todo, url: taskslist_todo_path(taskslist_id: @taskslist.id, id: @todo.id), method: :put do |f| %>

    <%= f.label :Edit_Substask %>
    <%= f.text_field :list %>

    <%= f.submit %>

    <% end %>


   
